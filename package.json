{
  "name": "solid-webhook-netlify",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "npm run generate:identity && tsc",
    "generate:identity": "node scripts/generate-identity.ts",
    "test": "vitest run",
    "test:unit": "vitest run tests/webhook.test.ts",
    "test:integration": "vitest run tests/integration",
    "dev": "netlify functions:serve --port 9999",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@netlify/functions": "^5.3.0",
    "@soid/core": "^0.1.0",
    "@solid/access-token-verifier": "^2.1.1",
    "jose": "^5.9.6",
    "jsonld": "^8.3.2",
    "n3": "^1.23.1"
  },
  "devDependencies": {
    "@types/jsonld": "^1.5.15",
    "@types/node": "^22.10.5",
    "typescript": "^5.7.2",
    "undici": "^6.21.0",
    "vite": "^6.0.7",
    "vitest": "^2.1.8"
  }
}
